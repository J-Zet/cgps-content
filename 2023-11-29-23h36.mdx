---
slug: "in-diesem-codeausschnitt-wird-berprft-ob-articlereadingtime-genau-1-ist"
title: "In diesem Codeausschnitt wird überprüft, ob article.readingTime genau 1 ist."
description: "Um den Text basierend auf dem Wert von readingTime anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können: Um den Text basierend auf dem Wert von readingTime anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können: Um den Text basierend auf dem Wert von readingTime anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:"
date: "2023-11-29T23:37:05.681Z"
readingTime: 4
---
# Herzlich Willkommen (Heading 1)

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Um den Text basierend auf dem Wert von `readingTime` anzupassen, können Sie eine einfache bedingte Logik (Ternär-Operator) verwenden. Hier ist ein Beispiel, wie Sie dies in Ihrem JSX-Code umsetzen können:

Dies ist eine **Demonstration** des Editors mit seinen Funktionalitäten.

> Dies hier stellt ein Zitat dar, welches im Editor via "Block Type" > "Quote" eingefügt werden kann.

Alle normalen Texte werden als "Paragraph" dargestellt.

## Einfügen von Links (Heading 2)

Ist über das Symbol "Create link" möglich
[Linktext (Link zu Google)](https://google.de/).

Um Bilder hinzuzufügen werden (siehe: "Insert image"), bitte **folgendes beachten!**&#x20;

* Bilder dürfen nur **maximal 2MB** groß sein\*\*.\*\*
* Nach dem Klick auf "Save" gibt es leider **kein Feedback - bitte nur 1 x klicken.**
* **Der Upload kann relativ lange dauern (bricht jedoch nach maximal 30 Sekunden ab). Die lange Upload-Dauer hat mit der "speziellen Technik" zu tun, mit der Daten auf der Webseite 'dynamisch' und dennoch ohne Datenbank hinzugefügt werden können.**

![](https://raw.githubusercontent.com/J-Zet/cgps-content/main/images/editor-example.png)

***

... diese Linie wird via "Insert thematic break" eingefügt.

### Listen (Heading 3)

* bulleted list
  * ......
  * ,,,,,,
* !!!!!!

***

1. numbered list
   1. ......
   2. ,,,,,,,
2. !!!!!!

***

* [ ] Check list - **Funktioniert leider noch nicht zuverlässig! Bitte nicht verwenden!**